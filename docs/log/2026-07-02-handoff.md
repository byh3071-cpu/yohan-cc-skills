# 2026-07-02 핸드오프 — Fable 5 멀티모델 오케스트레이션 도입 검토

> 상태: **반영 완료 (2026-07-03)** — 서브에이전트 tier 정렬(planner·critic→opus) + 독트린 3곳 통일 커밋됨(c38cfc0·d4f816f), next-task도 반영. Codex·cross-check 병렬통합만 별건 잔여. 실측 정정은 §5.
> ⚠️ 아래 §1~§4는 검토 당시(2026-07-02, 승인 대기 시점) 원문 — append-only 보존. 당시 "코드 변경 없음"은 그 시점 사실이며, 이후 실측·반영 내역은 §5 참조.

## 1. 지금까지 한 것

unclejobs.ai 스레드("Fable 5 토큰 아끼는 법 = Fable은 지휘만, 실행은 하위 모델 + Codex") 팩트체크 완료.

| # | 주장 | 판정 | 근거 |
|---|---|---|---|
| 1 | Fable 5가 현행 최고가 | 사실 | $10/$50 per 1M — Opus 4.8($5/$25)의 2배 |
| 2 | Fable 5는 액세스 승인 필요 | **거짓** | Fable 5는 GA. 승인제는 쌍둥이 Mythos 5(Project Glasswing 전용). 단 Fable 5도 30일 데이터 보존 요건 있음 |
| 3 | 서브에이전트 모델 고정 가능 | 사실 | frontmatter `model` 필드 — code.claude.com/docs/en/sub-agents |
| 4 | OpenAI 공식 Codex 플러그인 | 사실 | github.com/openai/codex-plugin-cc (openai 본계정, 2026-03-31 공개) |
| 5 | 설치 명령 문법 | 사실 | README 원문: `/plugin marketplace add openai/codex-plugin-cc` → `/plugin install codex@openai-codex` |
| 6 | 어드바이저 툴 존재 | 사실 | code.claude.com/docs/en/advisor + API `advisor_20260301`(beta) |
| 7 | 지휘자 컨텍스트 부풀면 입력 토큰 매 턴 반복 | 사실 | API stateless — 캐시 히트해도 읽기 ~0.1x 비용 지속 |

과금 확인: Codex 플러그인은 **ChatGPT 구독(Plus 포함) 로그인 시 추가 비용 0** — Plus의 Codex 한도에서 차감. API 키 쓰면 종량 과금. 우리는 Plus 보유 → 전자.

## 2. 핵심 결정 (제안 상태)

**원칙: 모델이 아니라 구조에 투자.** Fable 5도 1년 뒤 구형 — 모델 세대교체에도 안 바뀌는 것(단가 계층·검증 게이트·축적 자산)에 투자.

| 항목 | 판단 | 왜 |
|---|---|---|
| 서브에이전트 tier 고정 | 즉시 도입 | 버전이 아니라 **tier 이름**(haiku/sonnet/opus)으로 박아 세대교체 자동 승계. 현재 model 필드 없어서 전부 Fable 단가($50/1M 출력)로 상속 중 |
| 배분안 | explorer→haiku, shipper→sonnet, planner·critic→opus | 탐색은 기계적(90% 절감), 판단·검증은 강한 모델 유지 |
| CLAUDE.md 위임 규칙 | 부분 도입 | "탐색량 많거나 기계적 반복일 때만 위임". "Fable 코드 금지" 극단은 위임 오버헤드로 역효과 |
| Codex 플러그인 | 도입 OK | Plus 보유 확인으로 보류→상향. 추가 비용 0 |
| 고부담 결정 병렬 | 게이트에서만 | 같은 문제를 Opus 서브에이전트·Codex에 서로 모르게 병렬 질의(앵커링 방지) → Fable이 종합. 갈리는 지점 = 위험 지점. 일상 작업엔 오버헤드 |
| fail-loudly 규칙 | 필수 | Codex 실패(로그인 만료·한도 초과) 시 조용히 건너뛰지 말고 반드시 보고 — 1인 운영은 점검 스케줄이 아니라 알람 구조 |

관리 부담 실사: tier 고정은 사실상 0(도입 시 plugin version bump 1회만), Codex는 월 1회 미만(CLI 업데이트·재로그인·한도).

## 3. 다음 할 일 (승인 후, 우선순위 순)

1. `plugins/yohan-core/agents/{explorer,planner,critic,shipper}.md` frontmatter에 `model` 추가 (explorer→haiku, shipper→sonnet, planner·critic→opus)
2. yohan-core plugin version bump → `marketplace update` → `install` 재설치 (⚠️ [[plugin-install-vs-enable]] 함정: 같은 버전이면 cache 안 갱신)
3. Codex CLI 설치: `npm install -g @openai/codex` + ChatGPT Plus 로그인 (인터랙티브 — 사용자 직접, `! codex` 로)
4. `/plugin marketplace add openai/codex-plugin-cc` → `/plugin install codex@openai-codex`
5. cross-check/release-gate에 고부담 병렬 + fail-loudly 통합 (기존 스킬 내용 읽고 통합 지점 확정)
6. CLAUDE.md에 위임 규칙 박제 — 모델 이름 하드코딩 금지, tier로만

## 4. 산출물 포인터

- **진입점**: 이 문서. 승인 시 첫 수정 대상 → `plugins/yohan-core/agents/explorer.md:1` (frontmatter)
- 관련 파일:
  - `plugins/yohan-core/agents/explorer.md` · `planner.md` · `critic.md` · `shipper.md`
  - `plugins/yohan-core/` 내 plugin manifest (version 필드 — bump 대상)
  - `plugins/yohan-core/skills/cross-check/` (병렬 검증 통합 후보 지점)
- 외부 근거:
  - https://github.com/openai/codex-plugin-cc
  - https://code.claude.com/docs/en/advisor
  - https://code.claude.com/docs/en/sub-agents
  - https://code.claude.com/docs/en/discover-plugins

---

## 5. 정정 (2026-07-03 실측 — 원문 §1~§4 보존)

§1·§2·§3의 핵심 전제가 repo 실측과 달라 정정한다.

- **틀림:** "서브에이전트 model 필드 없어서 전부 Fable 단가($50/1M)로 상속 중."
- **실제:** 4개 agent 이미 tier 핀됨 — `explorer=haiku`, `planner/critic/shipper=sonnet`. 기본 지휘자도 Fable 아닌 **Opus 4.8 max**(`plugins/yohan-core/CLAUDE.md` 모델 전략). → $50 낭비 없음.
- **변경 범위 축소:** planner·critic만 `sonnet→opus`(2줄) + 독트린 3곳 정렬(CLAUDE.md·cost-guard·핀) + 순정 스위치. explorer·shipper는 이미 제안과 일치.
- **추가 발견(파생 미러):** `docs/ARCHITECTURE.md`·`docs/PRD.md`가 배분을 하드코딩 미러 → 동반 갱신함.
- **교훈:** ①핸드오프 전제는 무검증 신뢰 금지, repo 실측 우선. ②독트린 SoT가 3곳 분산되면 드리프트 → 정렬 필요.

실행 근거: `C:\Users\user\.claude\plans\fluffy-humming-church.md` (승인됨). 재설치(`/plugin marketplace update` + install)는 사용자·머신별. Codex·cross-check 통합은 별건.

---

## 6. 링크 박제 (2026-07-06 — 생태계 감사 후속, 원문 보존)

- §3(위 3번 항목)이 인용한 `[[plugin-install-vs-enable]]` 위키링크는 실제 패턴 문서가 없어 dangling 이었다(감사 F35). → `docs/patterns/PAT-005-plugin-install-vs-enable.md` 로 실체화·박제 완료. 앞으로 이 트랩은 PAT-005 참조.
